export const CardPhaseEngine=(()=>{
  const DEFAULT_STARTING_CRYSTALS=4,DEFAULT_MAX_CRYSTALS=10,DEFAULT_CRYSTAL_GROWTH=1,DEFAULT_HAND_SIZE=5;
  function create({deck=[],startingCrystals=DEFAULT_STARTING_CRYSTALS,maxCrystals=DEFAULT_MAX_CRYSTALS,crystalGrowth=DEFAULT_CRYSTAL_GROWTH,handSize=DEFAULT_HAND_SIZE}={}){return{zones:DeckEngine.create(deck),crystals:0,crystalCapacity:0,startingCrystals,maxCrystals,crystalGrowth,turnsStarted:0,handSize,active:false,played:[],mulliganAvailable:true,mulliganDone:false};}
  function expireTurnCards(state){const expired=[];for(const zoneName of ["hand","deck","discard"]){const zone=state?.zones?.[zoneName];if(!Array.isArray(zone))continue;for(let i=zone.length-1;i>=0;i--){const card=CardDatabase.get(zone[i]);if(card&&CardDatabase.lifetime(card)===CardDatabase.LIFETIME.TURN){expired.push(zone[i]);zone.splice(i,1);}}}return expired;}
  function begin(state,{handSize=state.handSize||DEFAULT_HAND_SIZE}={}){const expired=expireTurnCards(state);state.lastExpiredTurnCards=expired;state.active=true;state.crystalCapacity=Math.min(Number(state.maxCrystals||DEFAULT_MAX_CRYSTALS),Number(state.startingCrystals||DEFAULT_STARTING_CRYSTALS)+Math.max(0,Number(state.turnsStarted||0))*Number(state.crystalGrowth||DEFAULT_CRYSTAL_GROWTH));state.turnsStarted=Number(state.turnsStarted||0)+1;state.crystals=state.crystalCapacity;state.played=[];const missing=Math.max(0,Number(handSize||0)-state.zones.hand.length);return DeckEngine.draw(state.zones,missing);}
  function mulligan(state,cardIds=[]){if(!state?.active||!state.mulliganAvailable||state.mulliganDone)return[];const selected=[...new Set(cardIds)].filter(id=>state.zones.hand.includes(id)),held=[];selected.forEach(id=>{if(DeckEngine.removeFromHand(state.zones,id))held.push(id);});const drawn=DeckEngine.draw(state.zones,held.length);state.zones.deck.push(...held);DeckEngine.shuffle(state.zones);state.mulliganAvailable=false;state.mulliganDone=true;return drawn;}
  function keepOpeningHand(state){if(!state?.mulliganAvailable||state.mulliganDone)return false;state.mulliganAvailable=false;state.mulliganDone=true;return true;}
  function canPlay(state,card){return !!state.active&&!!card&&state.zones.hand.includes(card.id)&&state.crystals>=Number(card.cost||0);}
  function commit(state,card){if(!canPlay(state,card))return false;if(!DeckEngine.removeFromHand(state.zones,card.id))return false;state.crystals-=Number(card.cost||0);state.played.push(card.id);if(CardDatabase.isSpell(card))DeckEngine.toDiscard(state.zones,card.id);return true;}
  function characterDefeated(state,cardId){if(cardId)DeckEngine.toGraveyard(state.zones,cardId);}
  function resolveTurnEndEffects(state,{units=[],team="P"}={}){const results=[];for(const unit of units){if(!unit?.alive||unit.team!==team)continue;for(const passive of SkillDatabase.passiveList(unit.character?.passives)){const effect=passive.turnEndEffect;if(effect?.type!=="DRAW")continue;const drawn=DeckEngine.draw(state.zones,Math.max(0,Number(effect.count||0)));results.push({sourceId:passive.id,sourceName:passive.name,unitId:unit.id,drawn});}}return results;}
  function end(state){state.active=false;}
  return{DEFAULT_STARTING_CRYSTALS,DEFAULT_MAX_CRYSTALS,DEFAULT_CRYSTAL_GROWTH,DEFAULT_HAND_SIZE,create,begin,mulligan,keepOpeningHand,canPlay,commit,characterDefeated,resolveTurnEndEffects,expireTurnCards,end};
})();
globalThis.CardPhaseEngine=CardPhaseEngine;
